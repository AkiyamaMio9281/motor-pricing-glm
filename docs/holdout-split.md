# Holdout split

Generated by `scripts/holdout_report.py`. Do not edit by hand; re-run the script.

Computed from `model.holdout` and the GLM runs `R/export_predictions.R` fits on each split's training rows, frame md5 `2ecb69783bcd2387d8e00d6b88335ae2`, 678,013 policy-years. Python 3.14.7.

**Rule, fixed before measuring.** Validation uses the risk-group split chosen in D2-3: every fifth
risk group, a run of consecutive policy ids with identical rating factors, is held out. This
document measures what the split the plan named, by IDpol, would have cost. Validation run:
`priced_claims_risk_group_split`.

## Splitting by IDpol is splitting by row

IDpol is unique in the frame, so a split by IDpol is a split by row. The IDpol split here holds
out a fifth of rows by an md5 hash of the id, which any language reproduces.

| Split | Held-out rows | Priced claims | Reported claims | Risk groups on both sides | Held-out rows with a piece in training |
|---|---|---|---|---|---|
| risk group | 135,455 | 5,311 | 7,221 | 0 | 0 |
| IDpol | 135,196 | 5,273 | 7,197 | 28,862 | 29,930 |

Under the IDpol split, 29,930 held-out rows have another piece of their risk group in
training. They hold 14.7% of held-out exposure, 12.9% of held-out priced claims
and 18.4% of reported ones. The risk-group split has none by construction.

## What the pieces share

85,317 of 581,535 risk groups have two or more pieces. If the pieces were separate
periods, each would claim independently. Expected counts below use each piece's Poisson
probability of at least one claim from the GLM fitted on all rows.

| Claims | Groups where every piece has a claim | Expected if pieces claimed independently | Ratio |
|---|---|---|---|
| priced | 51 | 34.6 | 1.5 |
| reported | 1,828 | 105.3 | 17.4 |

Reported claims are shared. In 1,828 groups every piece reports a claim, 17 times what
independent pieces would give, and 1,707 of those groups have no amount on any piece.
Priced claims are much closer to independent, 51 against 34.6. The pattern is one claim count written onto every piece
of a policy-year, with the amount, where there is one, attached to a single piece.

This bears on D3-1's unpriced claims. In groups where every piece reports a claim and none has
an amount, counting the claims once, as the largest count on any one piece, removes
1,959 of the 9,658 claims without an amount, 20.3%. For vehicle age 0 it removes
1,344 of 4,024, 33.4%, and 37.8% of new-vehicle risk groups have two or more pieces
against 13.0% for older vehicles. That part of the unpriced claims looks like a repeated count
rather than a missing cost. The rest is still unexplained.

## The leak, measured on the same rows

A GLM with 76 parameters cannot remember a policy; a flexible model can. The memoriser stands in
for one. It multiplies the GLM's prediction by the actual over expected claims of training rows
with exactly the same nine rating factors, credibility-weighted as (claims + k) / (expected + k).
Both splits are scored on the 27,106 rows that both hold out, so the rows and their
outcomes are identical and only the training rows differ. Poisson deviance, lower is better.

Priced claims, 1,049 on these rows:

| Model | Risk-group split | IDpol split | IDpol minus risk group |
|---|---|---|---|
| constant | 6,820.5 | 6,820.6 | 0.0 |
| GLM | 6,479.8 | 6,479.3 | -0.5 |
| memoriser, k = 0.5 | 6,485.1 | 6,488.8 | 3.7 |
| memoriser, k = 2 | 6,480.9 | 6,480.8 | -0.1 |
| memoriser, k = 8 | 6,480.1 | 6,479.6 | -0.5 |

Reported claims, 1,454 on these rows:

| Model | Risk-group split | IDpol split | IDpol minus risk group |
|---|---|---|---|
| constant | 9,205.0 | 9,205.0 | 0.0 |
| GLM | 8,676.1 | 8,679.8 | 3.7 |
| memoriser, k = 0.5 | 8,670.7 | 8,401.4 | -269.3 |
| memoriser, k = 2 | 8,672.2 | 8,561.2 | -111.0 |
| memoriser, k = 8 | 8,674.7 | 8,643.1 | -31.6 |

| Claims | k | Memoriser minus GLM, risk-group split | Memoriser minus GLM, IDpol split | Handed to the memoriser by the IDpol split | As a share of the GLM's gain over a constant |
|---|---|---|---|---|---|
| priced | 0.5 | 5.2 | 9.5 | -4.2 | -1.2% |
| priced | 2 | 1.1 | 1.5 | -0.4 | -0.1% |
| priced | 8 | 0.2 | 0.3 | 0.0 | 0.0% |
| reported | 0.5 | -5.4 | -278.5 | 273.1 | 51.6% |
| reported | 2 | -3.9 | -118.6 | 114.7 | 21.7% |
| reported | 8 | -1.4 | -36.7 | 35.4 | 6.7% |

On reported claims the IDpol split hands the memoriser between 35.4 and 273.1 of deviance,
7% to 52% of everything the rating factors gain over a constant on the same rows (528.9).
On the risk-group split the same memoriser gains almost nothing. On priced claims the IDpol split
hands it nothing: -4.2, -0.4, 0.0 at the three values of k.
The GLM's own deviance differs between the splits by -0.5 on priced claims and 3.7 on reported ones.

Where the reported gain comes from, within the IDpol holdout, k = 2, deviance per 1,000 rows:

| Claims | Memoriser minus GLM, rows with a piece in training | Memoriser minus GLM, other rows |
|---|---|---|
| priced | -0.16 | -1.28 |
| reported | -17.59 | -1.49 |

On reported claims the gain sits on rows whose other pieces were in training, which is the
repeated claim count being read back. On priced claims those rows gain less than the rest.

## What each split reports for the pricing GLM

The plan expected two sets of metrics, one per split, and their difference to be the leak.

| Priced claims, each split's own holdout | Risk-group split | IDpol split |
|---|---|---|
| held-out rows | 135,455 | 135,196 |
| held-out claims | 5,311 | 5,273 |
| off-balance factor, from training rows | 0.999775 | 0.999930 |
| frequency deviance, GLM against constant | 4.91% lower | 4.85% lower |
| capped severity deviance, GLM against constant | 0.34% lower | 0.75% lower |
| capped losses, actual over expected | 1.005 | 1.006 |
| recorded losses, actual over expected | 0.977 | 1.244 |
| recorded losses without the largest claim | 0.977 | 0.886 |

For the GLM, on priced claims, there is no leak to find, and the columns differ by what each
holdout drew. The largest claim, 4,075,401, fell in the IDpol holdout: capped losses come in at
1.005 and 1.006 of expected, recorded losses at 0.977 and 1.244, and 0.886 without that claim. Read as
split against split, one claim would pass for a leak. That is why the leak above is measured on
rows both splits share.

## Handed on

- **D3-3:** LightGBM trains on the risk-group split's training rows, with recorded losses as the
  target. The leak measured here is in reported claim counts; a boosted model fitted to them
  with a row split would be able to read it back.
- **D3-4 to D3-6:** the holdout is `risk_group_holdout` and the GLM is `priced_claims_risk_group_split`,
  rebalanced on its training rows. Recorded-loss A/E on one holdout moves with a single claim, so
  calibration shows capped losses beside recorded ones, and the bootstrap resamples risk groups.
- **D4:** part of the unpriced claims on new vehicles are repeated counts, which narrows, without
  settling, the question of what the new-vehicle relativity should be.
